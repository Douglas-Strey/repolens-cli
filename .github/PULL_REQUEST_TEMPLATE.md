## What does this change?

<!-- One or two sentences. Link the issue it closes, e.g. "Closes #123". -->

## How was it tested?

<!-- New or updated tests, fixtures, and any manual runs (`pnpm dev -- ../some-project`). -->

## Checklist

- [ ] `pnpm lint`, `pnpm typecheck` and `pnpm test` pass
- [ ] New detection logic has tests (and a fixture when it reads files)
- [ ] No project code is executed and no secret values can reach the output
- [ ] Docs updated if behavior, output or JSON schema changed (`docs/json-schema.md`, `docs/diagnostics.md`)
- [ ] `CHANGELOG.md` has an entry under "Unreleased" for user-facing changes
